project('relpath', 'c')

assert(rel_path('foo/lib', 'foo/bin')        == '../lib', 'Relative path is broken')
assert(rel_path('/foo/lib', '/foo/bin')       == '../lib', 'Relative path is broken for absolute paths')

